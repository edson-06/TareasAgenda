<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.agenda_tareas">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Agenda_Tareas"
        android:usesCleartextTraffic="true">
        <activity android:name=".ListaMateriasDiaActivity"></activity>
        <activity android:name=".addHorarioActivity" />
        <activity android:name=".showMateriaActivity" />
        <activity android:name=".addMateriasActivity" />
        <activity android:name=".showPendienteActivity" />
        <activity android:name=".addPendientesActivity" />
        <activity android:name=".RegistrarActivity" />
        <activity android:name=".HorariosActivity" />
        <activity android:name=".MateriasActivity" />
        <activity android:name=".PendientesActivity" />
        <activity android:name=".MenuActivity2" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>